@page
@model Car_Store.Pages.addproductModel
@{
    Layout = "_Layout";
    string[] carAccessories = {
  "Air fresheners",
  "All-weather floor mats",
  "Car covers",
  "Cargo liners",
  "Dash cams",
  "Emergency kits",
  "Floor mats",
  "GPS systems",
  "Headrest DVD players",
  "License plate frames",
  "Phone mounts",
  "Roof racks",
  "Seat covers",
  "Steering wheel covers",
  "Sun shades",
  "Trailer hitches",
  "Wheel covers"
};
}


<link rel="stylesheet" href="~/css/Add-Product.css" asp-append-version="true">

<div class="my-page-product">
    <div class="my-card">
        <input type="file" id="car-image" name="car-image">
        <label for="upload-image">
            <i class="fa-sharp fa-solid fa-cloud-arrow-up"></i>
            Upload product image
        </label>
    </div>
    <div class="card">
        <form method="post" id="add-from">
            <div class="product-title">
                <label for="p-title">Add title</label>
                <input required id="p-title" type="text" class="my-input" placeholder="Ex: mobile holder" />
            </div>
            <div>
                <label for="p-Category">Category</label>
                <select required id="p-Category" class="my-select">
                    @foreach(string cateory in carAccessories)
                    {
                        <option value="@cateory">@cateory</option>
                    }
                </select>
            </div>
            <div class="flex-row-double">
                <div class="flex-item-d">
                    <label for="p-branch">branch</label>
                    <select required class="my-select" id="p-branch">
                        @for (int i = 1; i < 5; i++)
                        {
                            <option value="@i">@i</option>
                        }
                    </select>
                </div>
                <div class="flex-item-d">
                    <label for="p-quantity">Quantity</label>
                    <input required id="p-quantity" type="text" class="my-input" placeholder="Enter product quantity" />
                </div>
            </div>
            <div class="flex-row-double">
                <div class="flex-item-d">
                    <label for="p-brand">brand</label>
                    <input required id="p-brand" type="text" class="my-input" placeholder="Enter product brand" />
                </div>
                <div class="flex-item-d">
                    <label for="p-price">Price</label>
                    <input required id="p-price" type="text" class="my-input" placeholder="Enter product price" />
                </div>
            </div>
            
        </form>
    </div>
    <div>
        <input class="submit-btn" form="add-from" type="submit" value="Add product" />
    </div>
</div>
